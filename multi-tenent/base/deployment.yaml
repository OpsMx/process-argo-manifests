apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  selector:
    matchLabels:
      app: frontend-deployment
  template:
    metadata:
      labels:
        app: frontend-deployment
    spec:
      containers:
      - name: app
        image: paulbouwer/hello-kubernetes:latest
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
